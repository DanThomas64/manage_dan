# Default configuration settings for the application

# Application environment (used for environment-specific config loading)
environment = "development"

# How often the background monitor polls nb for changed todos/notes (seconds).
monitor_interval_secs = 30

# Local hour (0-23) at which the daily summary ticket is printed.
summary_hour = 8

# Detail level for the daily summary ticket.
# Options: "minimal" (overdue only), "standard" (overdue + high priority),
#          "full" (overdue + high priority + upcoming 7 days)
summary_level = "full"

# Whether to print an end-of-day summary of tasks completed during the day.
completed_summary_enabled = true

# Local hour (0-23) at which the completed-task summary is printed.
completed_summary_hour = 20

[printer]
# Vendor ID (VID) for the USB ESC/POS printer
# Default: 0x0fe6
vendor_id = 4070

# Product ID (PID) for the USB ESC/POS printer
# Default: 0x811e
product_id = 33054

# Printer backend: "usb" for physical thermal printer, "terminal" for stdout rendering
mode = "terminal"

# Characters per line of the physical receipt.  Common values: 42 (ESC/POS default) or 48.
# Check your printer's spec sheet and set this in config/local.toml if needed.
characters_per_line = 42

[logging]
# Path to the log file (relative to the working directory, or absolute).
# Default: data/logs/app.log  (relative to project root)
file = "data/logs/app.log"

[todo]
# nb notebook name todos are stored in (same nb CLI the notes/Log feature
# uses). Projects are approximated as subfolders inside this notebook
# (e.g. "work/", "personal/").
nb_notebook = "todo"

[project]
# Base directory project folders are created under. Supports a leading "~/"
# for the user's home directory.
base_dir = "~/projects"
