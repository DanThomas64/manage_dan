<?xml version="1.0" encoding="utf-8"?>
<!-- Allow plain HTTP for local network access (no public internet exposure) -->
<network-security-config>
    <base-config cleartextTrafficPermitted="true" />
</network-security-config>
