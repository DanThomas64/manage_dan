/target
.aider*
.termdbms/.2186236571916154720
*.log
app.sqlite
app.sqlite-journal
app.sqlite-wal
app.sqlite-shm
.termdbms

# Local config overrides — never commit these
config/local.toml
config/local.*.toml
config/local.toml_bak
config/recurring.toml
config/reminders.toml

# Runtime data (SQLite DB, log files)
/data/
